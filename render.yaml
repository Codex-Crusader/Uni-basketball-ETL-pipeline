services:
  - type: web
    name: ncaa-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --serve
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
