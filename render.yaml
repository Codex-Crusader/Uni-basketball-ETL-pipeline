services:
  - type: web
    name: ncaa-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --serve
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
